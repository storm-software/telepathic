# CLI Reference

Telepathic is operated via the CLI by running the `telepathic run` command. To
stop it, press `ctrl-c`. You may need to wait a bit as telepathic completes
cleanup tasks.

However, Telepathic has more commands:

{{#include ./SUMMARY.md}}
